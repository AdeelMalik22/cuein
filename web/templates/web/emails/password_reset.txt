Hi {{ user.get_full_name|default:user.username }},

We received a request to reset your Cuein password. Enter this six-digit code in Cuein to choose a new password:

{{ reset_code }}

This code expires in {{ password_reset_timeout_minutes }} minute{{ password_reset_timeout_minutes|pluralize }}. Never share it with anyone.

If you did not request a password reset, you can safely ignore this email. Your password will not change unless this code is used.

Cuein
